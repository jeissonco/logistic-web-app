# Mapping photos

Drop reference photos here so they show on the Mapping screen:

- `warehouse.jpg` — the warehouse / bunker (Brandon's Shredding Boxes)
- `home.jpg` — Brandon's home, 19 Westerway Terrace

If a file is missing the card just shows a camera placeholder. To add or change a
location, edit `PLACES` in `src/app/(app)/mapping/page.tsx`.
